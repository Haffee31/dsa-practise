# 36. Valid Sudoku
  
<br>**Problem:** https://leetcode.com/problems/valid-sudoku/<br>

**Difficulty:** Medium<br>
**Topics:** Array, Hash Table, Matrix<br>
**Language:** cpp<br>
**Status:** Accepted<br>
**Submitted:** 2026-05-22 00:36 local time

**Runtime:** 0 ms (beats 100%)
**Memory:** 21.3 MB (beats 99.4748%)


<!-- leetgit:submissionId=2009335385 codeHash=f91c0dae1fa481ca31d62560eec1b47ea3a6d75846bd4f7d0089ad8f5c24bd87 notesHash=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 -->

## Solution

```cpp
class Solution {
public:

    bool isSafe(vector<vector<char>>& board, int row, int col) {

        char digit = board[row][col];

        for(int i = 0; i < 9; i++) {

            // Check column
            if(i != row && board[i][col] == digit)
                return false;

            // Check row
            if(i != col && board[row][i] == digit)
                return false;

            // Check 3x3 box
            int boxRow = 3 * (row / 3) + (i / 3);
            int boxCol = 3 * (col / 3) + (i % 3);

            if((boxRow != row || boxCol != col) &&
                board[boxRow][boxCol] == digit)
                return false;
        }

        return true;
    }

    bool isValidSudoku(vector<vector<char>>& board) {

        for(int row = 0; row < 9; row++) {

            for(int col = 0; col < 9; col++) {

                if(board[row][col] != '.') {

                    if(!isSafe(board, row, col))
                        return false;
                }
            }
        }

        return true;
    }
};
```
