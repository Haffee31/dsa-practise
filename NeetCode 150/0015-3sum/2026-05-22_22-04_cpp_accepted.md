# 15. 3Sum
  
<br>**Problem:** https://leetcode.com/problems/3sum/<br>

**Difficulty:** Medium<br>
**Topics:** Array, Two Pointers, Sorting<br>
**Language:** cpp<br>
**Status:** Accepted<br>
**Submitted:** 2026-05-22 22:04 local time

**Runtime:** 43 ms (beats 84.52590000000001%)
**Memory:** 29 MB (beats 71.76350000000002%)


<!-- leetgit:submissionId=2010082045 codeHash=01810ba44d53757c4ade9f4a3452f58226a6f88819666bea5f4924e33fd5eec8 notesHash=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 -->

## Solution

```cpp
class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        // Sorting + 2 pointer approach - Optimal

        int n = nums.size();
        sort(nums.begin(), nums.end());

        vector<vector<int>> triplets;

        for(int i=0; i<n; i++){
            if(i > 0 && nums[i] == nums[i-1])
                continue;   // skip duplicate

            int j = i + 1, k = n - 1;

            while(j < k){
                int tripletSum = nums[i] + nums[j] + nums[k];

                if(tripletSum == 0){
                    triplets.push_back({nums[i], nums[j], nums[k]});
                    j++;
                    k--;
                    
                    // Skip duplicates
                    while(j < k && nums[j] == nums[j-1])
                        j++;
                    
                    while(j < k && nums[k] == nums[k+1])
                        k--;
                }
                else if(tripletSum > 0){
                    k--;
                }else{
                    j++;
                }
            }
        }

        return triplets;
    }
};
```
